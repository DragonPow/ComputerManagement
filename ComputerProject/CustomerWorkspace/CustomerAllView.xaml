<UserControl x:Class="ComputerProject.CustomerWorkspace.CustomerAllView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ComputerProject.CustomerWorkspace"
             mc:Ignorable="d" Padding="30" Background="{StaticResource Background}"
             d:DesignHeight="1000" d:DesignWidth="1300">
    <DockPanel>
        <TextBlock 
            Height="40"
            FontSize="28"
            DockPanel.Dock="Top" 
            FontFamily="{StaticResource Mulish}"
            FontWeight="SemiBold"
            Foreground="{StaticResource Title}" Margin="10,0,10,15">
            Khách hàng
        </TextBlock>
        <Grid
            Background="White"
            >
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <local:CustomerSearchView
                Grid.Row="0"
                Background="White"
                Margin="10,0" 
                />
            <local:Test
                Grid.Row="1"
                Margin="10,0"
                />
            <ListView
                Grid.Row="2"
                Margin="10,0"
                x:Name="ListCustomers">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <local:CustomerAllViewRow Loaded="CustomerAllViewRow_Loaded"/>
                    </DataTemplate>
                </ListView.ItemTemplate> 
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
        </Grid>
    </DockPanel>
</UserControl>
